import{useCallback,useEffect,useMemo,useState}from"react";import{addQuote,changeOrderStatus,createInstallments,listOrders,listSuppliers,receiveOrderItem,saveOrder}from"../services/compras.service";
export default function useComprasInteligentes({empresaId,userId}){const[orders,setOrders]=useState([]);const[suppliers,setSuppliers]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const refresh=useCallback(async()=>{if(!empresaId||!userId)return;setLoading(true);setError("");try{const[a,b]=await Promise.all([listOrders(empresaId),listSuppliers(userId)]);setOrders(a);setSuppliers(b)}catch(reason){setError(reason.message)}finally{setLoading(false)}},[empresaId,userId]);useEffect(()=>{const timer=window.setTimeout(refresh,0);return()=>window.clearTimeout(timer)},[refresh]);const metrics=useMemo(()=>{const open=orders.filter((o)=>!["Recebido","Cancelado"].includes(o.status));const received=orders.filter((o)=>o.status==="Recebido");const main=[...orders.reduce((map,o)=>map.set(o.fornecedor.nome,(map.get(o.fornecedor.nome)||0)+o.valorTotal),new Map()).entries()].sort((a,b)=>b[1]-a[1])[0]?.[0]||"—";return{count:orders.length,value:orders.reduce((s,o)=>s+o.valorTotal,0),weight:orders.flatMap((o)=>o.items).reduce((s,i)=>s+i.peso,0),ticket:orders.length?orders.reduce((s,o)=>s+o.valorTotal,0)/orders.length:0,main,lead:received.length?received.reduce((s,o)=>s+Math.max(0,(new Date(o.previsao)-new Date(o.data))/86400000),0)/received.length:0,open:open.length,pending:orders.filter((o)=>["Comprado","Recebido parcialmente"].includes(o.status)).length}},[orders]);async function run(action){await action();await refresh()}return{orders,suppliers,metrics,loading,error,refresh,save:(order)=>run(()=>saveOrder({empresaId,userId,order})),status:(order,status)=>run(()=>changeOrderStatus({order,empresaId,userId,status})),quote:(orderId,quote)=>run(()=>addQuote({orderId,empresaId,userId,quote})),installments:(order,count,firstDue)=>run(()=>createInstallments({order,empresaId,userId,count,firstDue})),receive:(itemId,quantity)=>run(()=>receiveOrderItem({itemId,empresaId,quantity}))}}
