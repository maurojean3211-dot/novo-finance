const fields = [["client", "Nome do cliente"], ["company", "Empresa"], ["phone", "Telefone"], ["whatsapp", "WhatsApp"], ["email", "E-mail", "email"], ["city", "Cidade"], ["state", "Estado"], ["country", "País"], ["product", "Produto/material"], ["quantity", "Quantidade", "number"], ["deadline", "Prazo desejado", "date"]];

export default function AttendanceForm({ form, onChange }) {
  const update = (field, value) => onChange({ ...form, [field]: value });
  return <section className="attendance-card attendance-form"><header><span>Entrada manual</span><h2>Informações do atendimento</h2><p>Preencha apenas o que o cliente realmente informou.</p></header><div className="attendance-fields">{fields.map(([field, label, type = "text"]) => <label key={field}><span>{label}</span><input type={type} value={form[field]} onChange={(event) => update(field, event.target.value)} /></label>)}<label><span>Unidade</span><select value={form.unit} onChange={(event) => update("unit", event.target.value)}><option>kg</option><option>tonelada</option><option>unidade</option><option>barra</option><option>metro</option></select></label><label className="wide"><span>Mensagem recebida</span><textarea value={form.message} onChange={(event) => update("message", event.target.value)} placeholder="Cole ou digite a mensagem do cliente. O texto não será enviado para serviços externos." /></label><label className="wide"><span>Observações</span><textarea value={form.notes} onChange={(event) => update("notes", event.target.value)} /></label></div></section>;
}
