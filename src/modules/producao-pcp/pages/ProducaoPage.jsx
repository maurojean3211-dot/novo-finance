import { useMemo, useState } from "react";
import { EmptyState, FeedbackBanner, MetricGrid, ModuleHeader, OperationModal } from "../../../components/operations/OperationsUI";
import useProducao from "../hooks/useProducao";
import { PRODUCTION_STATUSES, addMaterial, budgetDraft, changeOrderStatus, consumeMaterial, createOrder, finishProduct, preparePurchaseNeed, recordProduction, releaseMaterial, reserveMaterial, reverseConsumption, saleDraft } from "../services/producao.service";
import "../producao.css";

const today=()=>new Date().toISOString().slice(0,10);const number=(value)=>Number(value||0);const emptyOrder=()=>({client:"",product:"",description:"",alloy:"",temper:"",dimension:"",quantity:"",unit:"kg",weight:"",start:today(),end:"",priority:"Média",responsible:"",notes:""});
export default function ProducaoPage({empresaId,userId,onNavigate}){const manager=useProducao(empresaId);const[tab,setTab]=useState("orders");const[form,setForm]=useState(null);const[selected,setSelected]=useState(null);const[feedback,setFeedback]=useState(null);const[busy,setBusy]=useState(false);const current=selected?manager.orders.find((item)=>item.id===selected.id)||selected:null;
const stockById=useMemo(()=>new Map(manager.stock.map((item)=>[item.id,item])),[manager.stock]);async function run(work,message){setBusy(true);setFeedback(null);try{await work();setFeedback({type:"success",message});await manager.refresh();return true}catch(error){setFeedback({type:"error",message:error.message||"Operação não concluída."});return false}finally{setBusy(false)}}async function save(){if(!form.product||number(form.quantity)<=0)return setFeedback({type:"error",message:"Produto e quantidade planejada são obrigatórios."});if(!window.confirm("Confirmar criação desta Ordem de Produção?"))return;const ok=await run(()=>createOrder({empresaId,userId,order:form}),"Ordem de Produção criada após confirmação humana.");if(ok)setForm(null)}
const metrics=[{label:"OPs abertas",value:manager.open.length,detail:"planejamento e execução",icon:"▤"},{label:"Em produção",value:manager.inProduction.length,detail:"execução atual",icon:"▶",tone:"green"},{label:"Atrasadas",value:manager.delayed.length,detail:"condição derivada",icon:"!",tone:"rose"},{label:"Concluídas",value:manager.completed.length,detail:"histórico real",icon:"✓",tone:"green"},{label:"Qtd. planejada",value:manager.planned.toLocaleString("pt-BR"),detail:"todas as OPs",icon:"#"},{label:"Qtd. produzida",value:manager.produced.toLocaleString("pt-BR"),detail:"apontamentos",icon:"↗"},{label:"Peso planejado",value:`${manager.plannedWeight.toLocaleString("pt-BR")} kg`,detail:"carga prevista",icon:"⚖"},{label:"Peso produzido",value:`${manager.producedWeight.toLocaleString("pt-BR")} kg`,detail:"produção real",icon:"◎"},{label:"Perdas",value:manager.losses.toLocaleString("pt-BR"),detail:"refugo apontado",icon:"×",tone:"amber"},{label:"Taxa de conclusão",value:manager.orders.length?`${(manager.completed.length/manager.orders.length*100).toFixed(1)}%`:"0%",detail:"OPs concluídas",icon:"%"},{label:"Materiais faltantes",value:manager.missing.length,detail:"necessidade de compra",icon:"▧",tone:"rose"}];
return <main className="ops-page production-page"><ModuleHeader eyebrow="Operação industrial" title="Produção e PCP" description="Ordens, materiais, apontamentos e planejamento conectados ao estoque real." actionLabel="Nova OP" onAction={()=>setForm(emptyOrder())}/><FeedbackBanner feedback={feedback} onClose={()=>setFeedback(null)}/>{manager.error&&<div className="ops-status-panel">{manager.error} A migration local da Fase 21 precisa ser aplicada.</div>}{manager.loading&&<div className="ops-status-panel">Carregando Produção e PCP...</div>}<MetricGrid items={metrics}/><nav className="production-tabs">{[["orders","Ordens de Produção"],["pcp","PCP"],["origins","Origens comerciais"]].map(([key,label])=><button key={key} className={tab===key?"active":""} onClick={()=>setTab(key)}>{label}</button>)}</nav>
{tab==="orders"&&<Orders orders={manager.orders} delayed={manager.delayed} onSelect={setSelected}/>} {tab==="pcp"&&<Pcp manager={manager} stockById={stockById} onSelect={setSelected} onNavigate={onNavigate}/>} {tab==="origins"&&<Origins manager={manager} onCreate={(draft)=>setForm({...emptyOrder(),...draft})}/>} {form&&<OrderModal form={form} setForm={setForm} onClose={()=>setForm(null)} onSave={save} busy={busy}/>} {current&&<OrderDetails order={current} manager={manager} empresaId={empresaId} userId={userId} stockById={stockById} busy={busy} run={run} onClose={()=>setSelected(null)} onNavigate={onNavigate}/>}</main>}
function Orders({orders,delayed,onSelect}){if(!orders.length)return <section className="ops-panel"><EmptyState title="Nenhuma Ordem de Produção" description="Crie manualmente ou revise uma origem comercial."/></section>;const delayedIds=new Set(delayed.map((item)=>item.id));return <section className="ops-panel"><div className="ops-panel__header"><h2>Ordens de Produção</h2><span>{orders.length} registro(s)</span></div><div className="ops-table-wrap"><table className="ops-table production-table"><thead><tr><th>OP</th><th>Produto</th><th>Cliente / origem</th><th>Prioridade</th><th>Previsão</th><th>Planejado</th><th>Produzido</th><th>Status</th><th></th></tr></thead><tbody>{orders.map((item)=><tr key={item.id} className={delayedIds.has(item.id)?"is-delayed":""}><td><strong>{item.numero_op}</strong>{delayedIds.has(item.id)&&<small>Atrasada</small>}</td><td><strong>{item.produto}</strong><small>{[item.liga,item.tempera,item.dimensao].filter(Boolean).join(" · ")}</small></td><td>{item.cliente_nome||"—"}<small>{item.venda_id?`Venda ${item.venda_id}`:item.orcamento_id?"Orçamento aprovado":"Manual"}</small></td><td>{item.prioridade}</td><td>{item.data_prevista_inicio||"—"} → {item.data_prevista_fim||"—"}</td><td>{item.quantidade_planejada} {item.unidade}</td><td>{item.quantidade_produzida} {item.unidade}</td><td>{item.status}</td><td><button onClick={()=>onSelect(item)}>Gerenciar</button></td></tr>)}</tbody></table></div></section>}
function Pcp({manager,stockById,onSelect,onNavigate}){return <section className="pcp-grid"><article><h2>Programação</h2>{manager.open.length?manager.open.sort((a,b)=>String(a.data_prevista_inicio||"9999").localeCompare(String(b.data_prevista_inicio||"9999"))).map((item)=><button key={item.id} onClick={()=>onSelect(item)}><span>{item.numero_op} · {item.prioridade}</span><strong>{item.produto}</strong><small>{item.data_prevista_inicio||"Sem início"} → {item.data_prevista_fim||"Sem término"}</small></button>):<p>Aguardando planejamento.</p>}</article><article><h2>Materiais faltantes</h2>{manager.missing.length?manager.missing.map(({order,material})=>{const stock=stockById.get(material.estoque_id);return <div key={material.id}><span>{order.numero_op}</span><strong>{material.material}</strong><small>Necessário {number(material.quantidade_prevista)-number(material.quantidade_reservada)-number(material.quantidade_consumida)} · disponível {stock?.estoque_disponivel||0}</small><button onClick={()=>onSelect(order)}>Revisar OP</button></div>}):<p>Nenhum bloqueio material identificado.</p>}<button className="pcp-purchase" onClick={()=>onNavigate("compras")}>Abrir Compras Inteligentes →</button></article></section>}
function Origins({manager,onCreate}){return <section className="production-origins"><header><h2>Criação confirmada a partir de origem</h2><span>Nenhuma OP é criada automaticamente.</span></header>{manager.sales.map((sale)=><article key={`s-${sale.id}`}><div><small>Venda</small><strong>{sale.cliente_nome} · {sale.produto}</strong><span>{sale.kilos||0} kg · {sale.data_venda}</span></div><button onClick={()=>window.confirm("Preparar OP a partir desta venda? Você ainda revisará os dados antes de salvar.")&&onCreate(saleDraft(sale))}>Preparar OP</button></article>)}{manager.budgets.map((budget)=><article key={`b-${budget.id}`}><div><small>Orçamento aprovado</small><strong>{budget.numero} · {budget.cliente_snapshot?.nome||"Cliente"}</strong><span>{budget.orcamento_itens?.length||0} item(ns)</span></div><button onClick={()=>window.confirm("Preparar OP a partir deste orçamento aprovado?")&&onCreate(budgetDraft(budget))}>Preparar OP</button></article>)}{!manager.sales.length&&!manager.budgets.length&&<EmptyState/>}</section>}
function OrderModal({form,setForm,onClose,onSave,busy}){const field=(key,label,type="text")=><label className="ops-field" key={key}><span>{label}</span><input type={type} value={form[key]||""} onChange={(event)=>setForm({...form,[key]:event.target.value})}/></label>;return <OperationModal title="Ordem de Produção" onClose={onClose} onSubmit={onSave} submitLabel="Confirmar OP" disabled={busy}>{field("client","Cliente")}{field("product","Produto")}{field("description","Descrição")}{field("alloy","Liga")}{field("temper","Têmpera")}{field("dimension","Dimensão")}{field("quantity","Quantidade planejada","number")}{field("unit","Unidade")}{field("weight","Peso planejado","number")}{field("start","Início previsto","date")}{field("end","Fim previsto","date")}<label className="ops-field"><span>Prioridade</span><select value={form.priority} onChange={(e)=>setForm({...form,priority:e.target.value})}><option>Baixa</option><option>Média</option><option>Alta</option><option>Urgente</option></select></label>{field("responsible","Responsável")}<div className="ops-preview">Origem: {form.saleId?`Venda ${form.saleId}`:form.budgetId?"Orçamento aprovado":"Manual"}. A gravação depende de confirmação humana.</div></OperationModal>}
function OrderDetails({order,manager,empresaId,userId,stockById,busy,run,onClose,onNavigate}){async function status(value){if(!window.confirm(`Confirmar mudança da ${order.numero_op} para ${value}?`))return;await run(()=>changeOrderStatus({empresaId,userId,order,status:value}),`OP atualizada para ${value}.`)}async function material(){const id=prompt("ID do item de estoque (copie da lista exibida):",manager.stock[0]?.id||"");const stock=manager.stock.find((item)=>item.id===id);if(!stock)return;const quantity=prompt(`Quantidade prevista de ${stock.codigo}:`,"1");if(quantity&&window.confirm("Adicionar este material ao planejamento da OP?"))await run(()=>addMaterial({empresaId,userId,orderId:order.id,stock,quantity}),"Material adicionado ao planejamento.")}async function point(loss=false){const quantity=prompt(loss?"Quantidade perdida:":"Quantidade produzida:","0");const weight=prompt(loss?"Peso perdido (kg):":"Peso produzido (kg):","0");if(quantity===null||weight===null)return;const reason=loss?prompt("Motivo: Qualidade, Processo, Matéria-prima, Equipamento, Medida ou Outro","Processo"):null;if(window.confirm("Confirmar este apontamento imutável?"))await run(()=>recordProduction({empresaId,userId,order,entry:{type:loss?"Perda":"Produção",quantity,weight,reason,notes:"Apontamento confirmado."}}),loss?"Perda registrada.":"Produção apontada.")}return <div className="production-overlay"><aside className="production-details"><header><div><small>{order.status}</small><h2>{order.numero_op}</h2><p>{order.produto} · {order.cliente_nome||"produção interna"}</p></div><button onClick={onClose}>×</button></header><section className="production-status"><select value={order.status} onChange={(e)=>status(e.target.value)} disabled={busy}>{PRODUCTION_STATUSES.map((item)=><option key={item}>{item}</option>)}</select><button disabled={!['Em produção','Pausada'].includes(order.status)||busy} onClick={()=>point(false)}>Apontar produção</button><button disabled={!['Em produção','Pausada'].includes(order.status)||busy} onClick={()=>point(true)}>Registrar perda</button></section><section><div className="production-section-title"><h3>Materiais</h3><button onClick={material}>Adicionar do estoque</button></div><div className="production-stock-reference">{manager.stock.slice(0,8).map((item)=><small key={item.id}>{item.id} · {item.codigo} · disponível {item.estoque_disponivel}</small>)}</div>{(order.ordem_producao_materiais||[]).map((item)=>{const stock=stockById.get(item.estoque_id);const missing=number(item.quantidade_prevista)-number(item.quantidade_reservada)-number(item.quantidade_consumida)>number(stock?.estoque_disponivel||0);const consumption=manager.movements.find((move)=>move.tipo==="Saída"&&String(move.origem_id||"").includes(item.id)&&!manager.movements.some((other)=>other.reversao_de===move.id));return <article className="production-material" key={item.id}><div><strong>{item.material}</strong><small>Previsto {item.quantidade_prevista} · reservado {item.quantidade_reservada} · consumido {item.quantidade_consumida} · disponível {stock?.estoque_disponivel||0}</small></div><div><button onClick={async()=>{const q=prompt("Quantidade a reservar:",String(Math.max(0,number(item.quantidade_prevista)-number(item.quantidade_reservada)-number(item.quantidade_consumida))));if(q&&window.confirm("Confirmar reserva no estoque?"))await run(()=>reserveMaterial({empresaId,materialId:item.id,quantity:q}),"Material reservado.")}}>Reservar</button><button disabled={!['Em produção','Pausada'].includes(order.status)} onClick={async()=>{const q=prompt("Quantidade consumida:",String(item.quantidade_reservada||1));if(q&&window.confirm("Confirmar consumo e saída do estoque?"))await run(()=>consumeMaterial({empresaId,materialId:item.id,quantity:q}),"Consumo registrado no estoque.")}}>Consumir</button><button disabled={!number(item.quantidade_reservada)} onClick={async()=>{const q=prompt("Quantidade a liberar:",String(item.quantidade_reservada));if(q&&window.confirm("Liberar esta reserva?"))await run(()=>releaseMaterial({empresaId,materialId:item.id,quantity:q}),"Reserva liberada.")}}>Liberar</button>{consumption&&<button onClick={()=>window.confirm("Reverter este consumo sem apagar a movimentação original?")&&run(()=>reverseConsumption({empresaId,materialId:item.id,movementId:consumption.id}),"Consumo revertido com histórico.")}>Reverter consumo</button>}{missing&&<button className="need" onClick={async()=>{if(window.confirm("Preparar necessidade para Compras Inteligentes?")){await run(()=>preparePurchaseNeed({empresaId,userId,orderId:order.id,materialId:item.id}),"Necessidade de compra preparada.");onNavigate("compras")}}}>Necessidade de compra</button>}</div></article>})}</section><section><div className="production-section-title"><h3>Produto acabado</h3></div><button disabled={order.status!=="Concluída"||order.entrada_produto_acabado_em||busy} onClick={async()=>{const stockId=prompt("ID do item de estoque do produto acabado:",manager.stock.find((item)=>String(item.produto_id)===String(order.produto_id))?.id||"");const quantity=prompt("Quantidade de entrada:",String(order.quantidade_produzida));if(stockId&&quantity&&window.confirm("Confirmar entrada única do produto acabado?"))await run(()=>finishProduct({empresaId,orderId:order.id,stockId,quantity}),"Produto acabado recebido no estoque.")}}>{order.entrada_produto_acabado_em?"Entrada já realizada":"Confirmar entrada no estoque"}</button></section><section><h3>Apontamentos e histórico</h3><div className="production-history">{[...(order.ordem_producao_apontamentos||[]),...(order.ordem_producao_historico||[])].sort((a,b)=>String(b.created_at).localeCompare(String(a.created_at))).map((item)=><p key={item.id}><strong>{item.tipo}</strong> · {item.descricao||item.observacoes||"Evento registrado"}<small>{new Date(item.created_at).toLocaleString("pt-BR")}</small></p>)}</div></section></aside></div>}
