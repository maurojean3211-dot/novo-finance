import { useState } from "react";
import { ACTIVITY_TYPES } from "../types/crm";
const money=(value)=>Number(value||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
export default function OpportunityDetails({opportunity,onClose,onEdit,onCreateQuote,onAddActivity,onDelete}){const[tipo,setTipo]=useState(ACTIVITY_TYPES[0]);const[descricao,setDescricao]=useState("");if(!opportunity)return null;function register(){if(!descricao.trim())return;onAddActivity(opportunity.id,{tipo,descricao:descricao.trim()});setDescricao("");}const rows=[["Contato",opportunity.contatoPrincipal],["Telefone",opportunity.telefone],["WhatsApp",opportunity.whatsapp],["E-mail",opportunity.email],["Localidade",[opportunity.cidade,opportunity.estado,opportunity.pais].filter(Boolean).join("/")],["Segmento",opportunity.segmento],["Origem",opportunity.origemLead],["Responsável",opportunity.vendedorResponsavel],["Previsão",opportunity.proximoContato],["Prioridade",opportunity.prioridade]];return <div className="crm-details-overlay" onMouseDown={(event)=>event.target===event.currentTarget&&onClose()}><aside className="crm-details"><header><div><small>{opportunity.etapa}</small><h2>{opportunity.empresa}</h2><p>{opportunity.produtoInteresse}</p></div><button onClick={onClose}>×</button></header><div className="crm-details__actions"><button onClick={()=>onEdit(opportunity)}>Editar oportunidade</button><button onClick={()=>onCreateQuote(opportunity)}>Criar orçamento</button><button className="danger" onClick={onDelete}>Excluir oportunidade</button></div><section className="crm-details__summary"><article><span>Valor estimado</span><strong>{money(opportunity.valorEstimado)}</strong></article><article><span>Probabilidade</span><strong>{opportunity.probabilidade}%</strong></article><article><span>Quantidade</span><strong>{opportunity.quantidade||"—"} {opportunity.unidade||""}</strong></article></section><section className="crm-detail-grid">{rows.map(([label,value])=><div key={label}><span>{label}</span><strong>{value||"—"}</strong></div>)}</section><section className="crm-notes"><h3>Observações</h3><p>{opportunity.observacoes||"Nenhuma observação registrada."}</p></section><section className="crm-activity"><div><h3>Histórico e atividades</h3><small>Histórico persistente</small></div><div className="crm-activity__form"><select value={tipo} onChange={(event)=>setTipo(event.target.value)}>{ACTIVITY_TYPES.map((value)=><option key={value}>{value}</option>)}</select><input placeholder="Descreva a interação" value={descricao} onChange={(event)=>setDescricao(event.target.value)}/><button onClick={register}>Registrar</button></div><div className="crm-timeline">{opportunity.atividades.length?opportunity.atividades.map((activity)=><article key={activity.id}><span>•</span><div><strong>{activity.tipo}</strong><p>{activity.descricao}</p><small>{activity.data}</small></div></article>):<p className="crm-no-activity">Nenhuma interação registrada.</p>}</div></section></aside></div>;}
