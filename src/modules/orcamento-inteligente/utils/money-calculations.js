import { calculateCommission, COMMISSION_TYPES, getCommissionRule } from "../../../services/commissionEngine";
export function formatMoney(value){return Number(value||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}
export function calculateQuoteCommission(items){return items.reduce((sum,item)=>{const subtotal=Number(item.preco||0)*Number(item.quantidade||0);const rule=getCommissionRule(item.descricao||item.codigo);if(rule.type===COMMISSION_TYPES.PERCENT_SALE)return sum+subtotal*(rule.rate/100);return sum+calculateCommission({product:item.descricao||item.codigo,quantity:item.pesoTotal||item.quantidade,unit:"KG",pricePerKg:item.preco}).commission},0)}
export function calculateQuoteTotals(items,{desconto=0,impostos=0}={}){const subtotal=items.reduce((sum,item)=>sum+Number(item.preco||0)*Number(item.quantidade||0),0);const descontoValor=subtotal*(Number(desconto||0)/100);const comissao=calculateQuoteCommission(items);return{subtotal,desconto:descontoValor,impostos:Number(impostos||0),comissao,valorFinal:subtotal-descontoValor+Number(impostos||0)+comissao}}
