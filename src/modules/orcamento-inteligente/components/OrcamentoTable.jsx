import { EmptyState } from "../../../components/operations/OperationsUI";
import { formatMoney } from "../utils/money-calculations";
export default function OrcamentoTable({ quotes, onOpen }) { return <section className="ops-panel"><div className="ops-panel__header"><h2>Orçamentos</h2><span>{quotes.length} resultado(s)</span></div>{quotes.length === 0 ? <EmptyState title="Nenhum orçamento encontrado"/> : <div className="ops-table-wrap"><table className="ops-table quote-table"><thead><tr><th>Número</th><th>Cliente</th><th>Status</th><th>Vendedor</th><th>Origem</th><th>Validade</th><th>Valor</th><th>Margem</th></tr></thead><tbody>{quotes.map((q) => <tr key={q.id} onClick={() => onOpen(q)}><td><strong>{q.id}</strong><small>{q.createdAt}</small></td><td>{q.cliente}</td><td><span className="quote-status">{q.status}</span></td><td>{q.vendedor}</td><td>{q.origem}</td><td>{q.validade}</td><td>{formatMoney(q.valor)}</td><td>{q.margem}%</td></tr>)}</tbody></table></div>}</section>; }
