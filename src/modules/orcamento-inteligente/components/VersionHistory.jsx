export default function VersionHistory({ history = [] }) { return <section className="ops-panel"><div className="ops-panel__header"><h2>Linha do tempo</h2><span>Histórico persistente</span></div><div className="quote-timeline">{history.length ? history.slice().sort((a,b)=>String(b.created_at).localeCompare(String(a.created_at))).map((event) => <article key={event.id}><span>●</span><div><strong>{event.tipo}</strong><p>{event.descricao}</p><small>{new Date(event.created_at).toLocaleString("pt-BR")}</small></div></article>) : <p>Nenhum evento registrado.</p>}</div></section>; }
