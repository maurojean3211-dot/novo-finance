import OrcamentoItemEditor from "./OrcamentoItemEditor";
import { formatMoney } from "../utils/money-calculations";
import CustomerSelector from "./CustomerSelector";
const fields = [["contato","Contato do orçamento"],["oportunidade","Oportunidade"],["vendedor","Vendedor"],["moeda","Moeda"],["validade","Validade","date"],["condicaoPagamento","Condição de pagamento"],["prazoEntrega","Prazo de entrega"],["modalidadeFrete","Modalidade de frete"]];
export default function OrcamentoForm({ editor, onSave, onCancel }) { const { quote, update, selectCustomer, addItem, removeItem } = editor; return <div className="quote-form"><CustomerSelector selectedId={quote.clienteId} snapshot={quote.clienteSnapshot} onSelect={selectCustomer}/><section className="ops-panel"><div className="ops-panel__header"><h2>Dados gerais</h2><span>Rascunho local com snapshot do cliente</span></div><div className="quote-form__grid">{fields.map(([key,label,type]) => <label className="ops-field" key={key}><span>{label}</span><input type={type || "text"} value={quote[key]} onChange={(e)=>update(key,e.target.value)}/></label>)}<label className="ops-field ops-field--wide"><span>Observações internas</span><textarea value={quote.observacoesInternas} onChange={(e)=>update("observacoesInternas",e.target.value)}/></label><label className="ops-field ops-field--wide"><span>Observações ao cliente</span><textarea value={quote.observacoesCliente} onChange={(e)=>update("observacoesCliente",e.target.value)}/></label></div></section><OrcamentoItemEditor onAdd={addItem}/><section className="ops-panel"><div className="ops-panel__header"><h2>Itens incluídos</h2><span>{quote.items.length} item(ns)</span></div><div className="quote-items">{quote.items.map((item)=><article key={item.id}><div><strong>{item.codigo || "Sem código"} · {item.descricao}</strong><small>{item.liga} {item.tempera} · {item.quantidade} {item.unidade} · {item.pesoTotal || 0} kg</small></div><span>{formatMoney(Number(item.preco)*Number(item.quantidade))}</span><button onClick={()=>removeItem(item.id)}>Remover</button></article>)}</div></section><footer><button onClick={onCancel}>Cancelar</button><button onClick={()=>onSave(quote)} disabled={!quote.clienteId}>Salvar orçamento local</button></footer></div>; }
