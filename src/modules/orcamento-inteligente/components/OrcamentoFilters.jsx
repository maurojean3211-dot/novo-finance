import { FilterBar } from "../../../components/operations/OperationsUI";
import { QUOTE_STATUSES } from "../types/orcamento";
export default function OrcamentoFilters({ filters, onChange, onClear, quotes }) { const unique = (key) => [...new Set(quotes.map((q) => q[key]).filter(Boolean))]; return <FilterBar><input placeholder="Pesquisar número ou cliente" value={filters.search} onChange={(e) => onChange({ ...filters, search: e.target.value })}/><select value={filters.cliente} onChange={(e) => onChange({ ...filters, cliente: e.target.value })}><option value="">Todos os clientes</option>{unique("cliente").map((v) => <option key={v}>{v}</option>)}</select><select value={filters.vendedor} onChange={(e) => onChange({ ...filters, vendedor: e.target.value })}><option value="">Todos os vendedores</option>{unique("vendedor").map((v) => <option key={v}>{v}</option>)}</select><select value={filters.status} onChange={(e) => onChange({ ...filters, status: e.target.value })}><option value="">Todos os status</option>{QUOTE_STATUSES.map((v) => <option key={v}>{v}</option>)}</select><select value={filters.origem} onChange={(e) => onChange({ ...filters, origem: e.target.value })}><option value="">Todas as origens</option>{unique("origem").map((v) => <option key={v}>{v}</option>)}</select><input type="date" value={filters.inicio} onChange={(e) => onChange({ ...filters, inicio: e.target.value })}/><input type="date" value={filters.fim} onChange={(e) => onChange({ ...filters, fim: e.target.value })}/><input type="date" aria-label="Validade" value={filters.validade} onChange={(e) => onChange({ ...filters, validade: e.target.value })}/><button onClick={onClear}>Limpar</button></FilterBar>; }
